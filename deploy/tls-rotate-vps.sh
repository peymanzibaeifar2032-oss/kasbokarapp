#!/bin/sh
# RETIRED. Do not use.
# This path decrypted /root/le-rotate.enc with a passphrase that was exposed
# in chat. Production TLS is CSR-split (key generated on the VPS).
echo "RETIRED_DO_NOT_USE deploy/tls-rotate-vps.sh"
exit 1
